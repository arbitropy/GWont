public enum BoardCell
{
    TopGreen, TopBlue, TopRed, BottomRed, BottomBlue, BottomGreen
}
public enum CardColor
{
    Red, Blue, Green
}

public enum BoardSide
{
    Top, Bottom, None // not sure if the none creates an issue, but only to indicate a draw
}

public enum PowerCardType
{
    AirSupport, Assassin, ClearWeather, Decoy, Medic, Morale, Spy, Weather, General
}
